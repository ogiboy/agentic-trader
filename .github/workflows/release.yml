name: Release

on:
  push:
    branches: [main, V1]
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: release-main
  cancel-in-progress: false

env:
  POETRY_VERSION: '2.3.4'
  PNPM_VERSION: '10.33.2'

jobs:
  release:
    name: Semantic Release
    runs-on: ubuntu-latest
    if: ${{ github.actor != 'github-actions[bot]' }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip

      - name: Set up pnpm
        uses: pnpm/action-setup@903f9c1a6ebcba6cf41d87230be49611ac97822e # v6.0.3
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          python -m pip install "poetry==${POETRY_VERSION}"

      - name: Install Node workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Install release dependencies
        run: |
          poetry config virtualenvs.in-project true
          poetry install --with dev --extras dev

      - name: Configure release Git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Validate package before release
        run: |
          poetry check --lock
          poetry run ruff check .
          poetry run pyright agentic_trader tests scripts
          poetry run python -m pytest -q
          pnpm run check:node
          pnpm run build:docs:pages

      - name: Detect release tag
        id: release-plan
        run: |
          set +e
          tag="$(
            poetry run semantic-release version --print-tag 2>/dev/null \
              | grep -E '^v[0-9]+(\.[0-9]+){2}([-.+][0-9A-Za-z.-]+)?$' \
              | tail -n 1
          )"
          status=$?
          set -e
          if [ "$status" -ne 0 ] || [ -z "$tag" ]; then
            echo "will_release=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "will_release=true" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

      - name: Bump version, changelog, and tag
        if: ${{ steps.release-plan.outputs.will_release == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: poetry run semantic-release version

      - name: Publish GitHub release
        if: ${{ steps.release-plan.outputs.will_release == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: poetry run semantic-release publish --tag "${{ steps.release-plan.outputs.tag }}"

      - name: Dispatch binary packaging
        if: ${{ steps.release-plan.outputs.will_release == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run binaries.yml --ref main -f tag="${{ steps.release-plan.outputs.tag }}"
