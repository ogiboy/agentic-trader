import { startRuntimeMonitors } from '../runtime-monitors.js';
import { startCamofoxServer } from '../server-bootstrap.js';

export function mountRuntimeMonitors({
  browserRestartsTotal,
  closeBrowserFully,
  closeSession,
  failuresTotal,
  getBrowser,
  getNativeMemBaseline,
  healthState,
  log,
  nativeMemRestartThresholdMb,
  refreshActiveTabsGauge,
  refreshTabLockQueueDepth,
  restartBrowser,
  safePageClose,
  scheduleBrowserIdleShutdown,
  sessionTimeoutMs,
  sessions,
  sessionsExpiredTotal,
  setNativeMemBaseline,
  tabInactivityMs,
  tabLocks,
  tabsReapedTotal,
}) {
  startRuntimeMonitors({
    browserRestartsTotal,
    closeBrowserFully,
    closeSession,
    failuresTotal,
    getBrowser,
    getNativeMemBaseline,
    healthState,
    log,
    nativeMemRestartThresholdMb,
    refreshActiveTabsGauge,
    refreshTabLockQueueDepth,
    restartBrowser,
    safePageClose,
    scheduleBrowserIdleShutdown,
    sessionTimeoutMs,
    sessions,
    setNativeMemBaseline,
    tabInactivityMs,
    tabLocks,
    tabsReapedTotal,
    sessionsExpiredTotal,
  });
}

export async function startServerBootstrap({
  app,
  authMiddleware,
  closeAllSessions,
  closeBrowserFully,
  closeSession,
  config,
  destroySession,
  ensureBrowser,
  failuresTotal,
  flyMachineId,
  getRegister,
  getResourceOpts,
  getSession,
  log,
  normalizeUserId,
  pluginEvents,
  proxyPool,
  refreshActiveTabsGauge,
  refreshTabLockQueueDepth,
  reporter,
  safeError,
  safePageClose,
  scheduleBrowserIdleShutdown,
  scheduleBrowserWarmRetry,
  sessions,
  setPluginContext,
  validateUrl,
  withUserLimit,
}) {
  return startCamofoxServer({
    app,
    authMiddleware,
    closeAllSessions,
    closeBrowserFully,
    closeSession,
    config,
    destroySession,
    ensureBrowser,
    failuresTotal,
    flyMachineId,
    getRegister,
    getResourceOpts,
    getSession,
    log,
    normalizeUserId,
    pluginEvents,
    proxyPool,
    refreshActiveTabsGauge,
    refreshTabLockQueueDepth,
    reporter,
    safeError,
    safePageClose,
    scheduleBrowserIdleShutdown,
    scheduleBrowserWarmRetry,
    sessions,
    setPluginContext,
    validateUrl,
    withUserLimit,
  });
}
